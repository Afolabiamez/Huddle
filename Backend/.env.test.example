# Copy to .env.test.local and configure a dedicated local PostgreSQL test database.
# The E2E runner never falls back to DATABASE_URL.
TEST_DATABASE_URL=postgresql://huddle:huddle_local_pw@localhost:5434/huddle_test
